

export default class Tax {
    id: number
    name: string
    rate: string


    constructor(id: number, name: string, rate: string) {
        this.id = id
        this.name = name
        this.rate = rate
    }

}